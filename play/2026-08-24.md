# Playtest 2026-08-24

## Headline-Befund: Die Langeweile-Stelle bleibt — `session_depth` 25, geführte Erschöpfung bei ~13 Aktionen. Die komplette entdeckbare Welt ist in einer Viertelstunde gezieltem Spiel da

Scorecard-Tag: **alle Metriken ±0** — deterministische Seeds + unveränderte Engine (kein Dev-Commit seit 22.08). Erwartete Flachheit, kein Regress-Signal. `recovery_stability` 0.375 im Band (Probe bis 03.09.), `warmth_stability` 0.46 (Probe bis 27.08.), `forage_pressure` 0.707 (Probe beendet 20.08, Entscheid beim Direktor).

Der wichtigste Befund ist unverändert der **boredom point**: ein kompetenter geführter Player leert alle 10 Blueprints + 5–9 Prozesse in **~9–16 Aktionen** (Volle Runs: exhaustion @9/@10/@10/@13/@14/@14/@16, **Median 13**). Danach `gather_at(random)` = reiner Grind. Die komplette entdeckbare Welt passt in **unter 20 gezielte Aktionen** — identisch zum 21.08. die Tier-2-Schicht (rope→cord_spear) verschiebt die Erschöpfung weiterhin **nicht** nach hinten, sie landet mitten in der Kette weil der Guide das survival-Gate automatisch öffnet. Der naive Random-Bot (session_depth 25) erreicht das Gate vor seinem Stall selten — jene zwei Blueprints bleiben ihm unsichtbar.

## Zweitbefund: guided-Bot bei 7/20 voll — innerhalb der dokumentierten fragilen Bandbreite, kein neuer Regress

Der Mess-Bot erreicht alle 10 Blueprints in **7/20** Seeds (21.08: 6/20, 19.08: 8/20) — das ist der bekannte RNG-Schwankungsband (~½–⅓ über mehrere Sessions), nicht ein Regress. Die 14 Sterbe-Runs gehen an Kälte+Verhungern (wie dokumentiert: der `not fire_active`-Rückzug-Trigger ist blind für ein aktives-aber-zu-schwaches Feuer am kalten Ort). Der aggressive body_temp<35 Retreat-Fix ist bekanntlich strikt schlechter (3/20, getestet 21.08) — **kein erneuter Versuch, bewusst nicht über-härtet.** Der 🔵 Tech-Debt dafür ist im BACKLOG notiert; ein chirurgischer Fix gehört in eine spätere Play-Tooling-Session, gegen-getestet über 20-Sweep.

## Naive Probe (eigener Run, seed 20260807, 60 Aktionen)

Findet **2 der 10 Blueprints** (knife_bone, knife_stone), **0 Prozesse**, Tier-2 nie. Items-Streu: 11 Templates gesammelt. Bestätigt die bekannte Struktur: der naive Blindflug trifft selten genug die ersten beiden Discoveries → survival-Gate (≥2 Tier-1) bleibt meist ungehoben → die zweite Discovery-Schicht fehlt aus naiv-Perspektive komplett. Das ist **Mess-Struktur (Peter/scorecard), kein Spiel-Feld.** Der session_depth-v2-Bot (ziel-bewusster, PLAN-Task) ist genau dafür gebaut — aktuell noch nicht umgesetzt.

## Scorecard-Referenz (2026-08-24, deterministisch)
- `session_depth` **25.0** (flach, 6. Messung) · `discovery_gap` **0.6** (Bandkante, naive 0.4/naive p25 0.3) · `craft_variety` 3.5 · `feedback_quality` 0.916 (NEAR_MISS-Blindstelle, bei Peter) · `blueprint_reachability` 1.0 (10/10) · `content_reachable` 1.0 (18/18) · `actions_to_first_craft` 34.5. Rest flach.

## Fazit für Direktor/Peter
- **Die Langeweile-Stelle bleibt 25.** Der einzige Hebel, der sie bewegt, sind die in PLAN.md versenkten, seit 22.08. freigegebenen Tasks: **Ehrlichmachungs-Batch** (skill_spread Label, craft_variety v2, content_reachable v2, feedback_quality v3), dann **session_depth v2** (ziel-bewusster Bot), dann **REC-002** (tool-aware reachability), dann **SPEC-006** (Werkzeug als Zutat). Keiner ist bisher in Dev umgesetzt (kein Dev-Commitl seit 22.08) → die Nordstern bleibt strukturell unbewegt, solange session_depth v1 (naiver Bot) und REC-002 die zweite Schicht zählen können.
- Beide Mess-Entscheide warten weiter auf Peter: feedback_quality/NEAR_MISS-Mapping + session_depth-Bot-Dedeutung.
- Keine Spielqualität-Regression: Verletzung im Band, guided-behandelt, Metriken pulsen nicht.

## Play-Tooling
- `play/guided_full.py` bleibt HEAD. 7/20 ist der dokumentierte fragile Zustand, kein neuer Regress.
- 🔵 Tech-Debt (bereits notiert 21.08): Rückzug-Trigger blind für warm-aktives-aber-unzureichendes Feuer am kalten Ort. Kein neuer Versuch heute.