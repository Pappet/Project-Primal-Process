# Play-Session — 2026-08-07

> Play-Agent. Ziel: das Spiel fühlen, nicht die Code-Korrektheit.
> Scorecard: `scorecard/2026-08-07.json` · Tests: 165 passed vor Start.

## Scorecard-Delta (vs 2026-08-05)

| Metrik | 05.08. | Jetzt | Δ | Richtung |
|--------|--------|-------|---|----------|
| actions_to_first_craft (v1) | 62 | 34.5 | −27.5 | ↑ besser — deutlich |
| blueprint_reachability (v1) | 1.000 | 0.750 | −0.25 | ↓ schlechter (⚠️ Artefakt, s.u.) |
| craft_variety (v1) | 1.000 | 3.000 | +2.0 | ↑ besser |
| skill_spread (v1) | 0.259 | 0.216 | −0.043 | ↓ schlechter |
| feedback_quality (v2) | 1.000 | 1.000 | ±0 | — |
| content_reachable (v1) | 1.000 | 1.000 | ±0 | — (13/13) |
| session_depth (v1) | 26 | 25 | −1 | ↓ schlechter |
| discovery_gap (v1) | 0.250 | 0.375 | +0.125 | im Band (⚠️ s.u.) |
| forage_pressure (v1, Probe) | — | 0.707 | Baseline | über Band 0.1–0.5 |

## 🔴 WICHTIGSTER BEFUND — die Langeweile-Stelle bleibt (session_depth ~25)

Die Entdeckungsmenge ist jetzt: **8 Blueprints + 4 Prozesse + 13 Item-Templates.**
Eine geführte Session leert sie **alle** bis tick ~96 (~40 Aktionen): knife, axe (3 Varianten), spear, spear_bound, alle 4 Prozesse, sharp_stone, tinder, fire_pit, cooked_meat. Danach ist schlicht **kein Ziel mehr übrig** — nichts, das eine neue Entdeckung verspricht. `session_depth`=25 sagt das als Zahl; gefühlt ist es stärker: mit den 8 Werkzeugen + kompletter Prozesskette ist das Discovery-Spiel einer einzigen ~40-Aktionen-Session fertig. SPEC-004 (Foraging) hat die *Sammel*-Tiefe leicht erhöht (`session_depth` p75 33→43), aber nicht die *Entdeckungs*-Tiefe — die Schere zwischen beiden bleibt die eigentliche Langeweile-Stelle.

## Was sich gut anfühlt
- **SPEC-002 hat den flint-Funnel massiv entschärft.** Naive Sessions bauen jetzt `knife_bone`, `knife_stone`, `axe_stone`, `spear` — nicht mehr nur flint-Werkzeuge. Bone (HARD), pebble (STONE), reeds+stick (spear) geben echte Alternativpfade. `craft_variety` 1.0→3.0 und naive Spieler kommen alle an Werkzeuge. Der mountain_peak-Hard-Gate von 05.08. ist gefühlt weg.
- **Spear ist per Familie sauber erreichbar** (reeds+stick via SHARP_OR_RIGID; rigid+fiber). Aha-Moment funktioniert.
- `actions_to_first_craft` 62→34.5 ist ein echter Gewinn: erste Craft landet jetzt deutlich früher — weniger tote Sammel-Schleife vor dem ersten Erfolgserlebnis.

## Was frustriert

### 🔴 B06/B07 weiterhin offen — beide reproduziert
- **`log_oak` (B06):** In der geführten Session mit frisch gebauter Axt gefällt → **"Unbekannt" ×2** im Inventar (Müll-Item, template_id=""). Kernversprechen "Axt bauen, um Holz zu fällen" bricht weiter. Template fehlt in `items.json` (bestätigt).
- **`clay_lump` (B07):** `SHOVEL`-Tag, das kein Item trägt → unerreichbar; Template fehlt ebenfalls. Doppelt tot, bestätigt.

### 🟡 Metric-Blindspot entdeckt — `blueprint_reachability` kann Tag-Familien nicht auflösen
`scorecard.py::_pair_slots` matcht Slot-Tags **literal** (`by_tag.get("SHARP_OR_RIGID")` → leer), kennt `TAG_FAMILIES` (`SHARP_OR_RIGID`, `RIGID_OR_FIBER`) nicht. Folge: `spear` und `spear_bound` melden **unreachable=false**, obwohl die Engine beide sauber craftet (verifiziert: alle 8 Blueprints SUCCESS). → `blueprint_reachability` gemeldet 0.75, **wahr 1.0**.
**Auswirkung auf discovery_gap:** Die Metrik wird mit unterschätztem Zähler gerechnet: gemeldet 0.375, wahr ≈ 1.0−0.375 = **0.625** (über Band-Obergrenze!). Die frühere Aussage "Gap komfortabel im Band (0.375)" ist damit wacklig — der wahre Abstand erreichbar-vs-gefunden ist größer als berichtet. **Das ist kein Spiel-Bug, sondern ein Zählfehler** — nicht still fixen (Metrik-Berechnung = braucht Peters Freigabe, Constitution), sondern flaggen und kalibrieren.

### 🟡 forage_pressure weiter über Band (0.707, Band 0.1–0.5)
Erstwert-Bestätigung der Probezeit-Notiz aus 06.08.: `stock < max_stock` als Schwellenwert zählt jeden Teilerfolg als Knappheit → Definitions-bedingt hoch. Kein Grind-Gefühl im kurzen Test, aber die Kalibrierung ist offen. Band/Definition braucht Direktor-Review (bis 20.08.).

## Fazit
SPEC-002 + SPEC-004 haben das *Sammel*-Spiel verbessert (erster Craft früher, mehr Werkzeug-Varianten, Rotation erzwungen). Die **Langeweile-Stelle ist unverändert die Entdeckungs-Leere**: alles Wissbare in ~40 Aktionen. Neu und wichtig: der gemeldete `discovery_gap` 0.375 ist durch einen Reachability-Zählfehler **untertrieben** — wahr eher ~0.625 (über Band). Damit ist SPEC-003 (partielle Match-Erkennung) weiterhin zu Recht ausgesetzt, aber aus dem *anderen* Grund: nicht Überführung, sondern der wahre Gap liegt am anderen Band-Rand. Kalibrierung des Zählers hat Vorrang vor jeder neuen Discovery-Mechanik.

## Einträge
- 🔴 B06/B07 bestätigt offen (keine neuen Einträge nötig, Status verifiziert)
- 🟡 Reachability-Tag-Familien-Blindspot → BACKLOG 🟡 Ideen (Metrik-Berechnung, braucht Peters Freigabe)
- 🟡 SPEC-003-Konflikt aktualisiert → JOURNAL + Report
- forage_pressure Probezeit-Kalibrierung bestätigt → BACKLOG 🟡
