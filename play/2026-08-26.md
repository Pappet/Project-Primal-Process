# Playtest 2026-08-26

## Headline-Befund: Erste PLAY-Lesung unter der v2-Re-Baseline — `session_depth` 64.5 ist eine neue Messleiste, keine längere Session. Der echte Boredom-Punkt bleibt ~15–22 gezielte Aktionen.

Scorecard: **alle Metriken ±0 außer der v2-Re-Baseline.** Am 25.08. ist der ziel-bewusste `session_depth`-v2-Bot gelandet (Dev, Probezeit bis 08.09.). Das ist eine **Re-Baseline, kein Spiel-Fortschritt** — das Spiel hat sich nicht geändert, nur der messende Bot. Die erste PLAY-Lesung bestätigt exakt die Dev-Vorgabe: `session_depth` liest jetzt **64.5** (p25 42 / p75 69) statt v1 25.0. **Nicht feiern.**

**Der Kern:** Der geführte kompetente Player leert die komplette entdeckbare Welt weiterhin in **~15–19 gezielten Aktionen** (volle 10-BP-Runs: last_new @14/@15/@16/@19, Median ~15; empfohlener 3-Seed-Scope @16). Die 64.5 sind die Aktionen, die der *messende naive* v2-Bot braucht, bis nichts Neues mehr passiert — Reise + Sammeln strecken ihn, nicht die Spieltiefe. Beide Perspektiven sagen dasselbe: **die Discovery-Welt bleibt ein flaches, kleines Content-Cap.** Der Boredom-Punkt hängt an der Zahl der discoverbaren Inhalte (10 Blueprints, 9 Prozesse, 10 Templates), nicht an Spielerkönnen. Der einzige echte Hebel ist die freigegebene **REC-002 → SPEC-006**-Kette (zweite Discovery-Schicht, Werkzeug-als-Zutat) — weiterhin offen in PLAN.md.

## Zweitbefund: guided-Bot 8/20 voll — dokumentierter fragiler Band, kein Regress

Volle 10-BP-Seeds: **8/20** (24.08: 7/20, 23.08: 6/20, 21.08: 6/20, 19.08: 8/20) — in der bekannten RNG-Schwankungsbreite (½–2 über Sessions), kein neuer Regress. Die übrigen ~12 sterben an Kälte+Verhungern (Energie 0, hp ≤ 0, body_temp ~12–30) — der bekannte Rückzug-Trigger erkennt ein *brennendes, aber für den kalten Ort unzureichendes* Feuer nicht (`not fire_active` blind). Nicht überhärten (aggressiver body_temp<35-Retreat-Fix war strikt schlechter, 3/20); chirurgischer Fix bleibt PLAYER-Tooling-Task in PLAN.md.

## Drittbefund: `discovery_gap` steht EXAKT auf der Bandkante 0.6

`discovery_gap` = **0.60, exakt die obere Bandgrenze (0.2–0.6)**. naive_discovery_rate 0.4, **naive_p25 0.3** — das untere Viertel der naiven Spieler findet nur wenig. Das ist exakt der annotierte Schwellwert ("Steigt der Gap über 0.6 → Spiel-Signal, Antwort spiel-seitig"). Kein Regress seit v1, aber der Rand ist heiß: Tier-2 (rope/cord_spear) bleibt für die naive Erste-2-Discovery-Fraktion Unsichtb. **Direktor-Runde: beobachten, nicht aufs Band optimieren, keine Metrik-Abschwächung.**

## Viertbefund (naive Probe, eigene Runs)

Blind-Runs (Seeds 20260826, 404): **sterben vor ~18–24 Aktionen** an Energie 0/Kälte (hp ≤ 0), hinterlassen **1–2 der 10 Blueprints** (knife/knife_stone), **0 Prozesse**, Tier-2 nie erreicht — obwohl der erste Craft schon bei Aktion ~2–4 glückt. Genau die bekannte naive-Struktur: Wer blind sammelt, sichert nie Feuer/Futter und kollabiert an der zweiten Leite. Der v2-Bot packt das Überleben gerade genug, um die lautere Reise-Nummer abzubilden — ein echter Zufalls-Michel stirbt. Der Real-Player muss Feuer/Nahrung früh lernen (Kälte+Hunger hart), das ist eine konstruierte Lern-Decke, kein Bug.

## Scorecard-Referenz (2026-08-26, deterministisch)
- `session_depth` **64.5 (v2, Re-Baseline, Probe bis 08.09.)** · `discovery_gap` **0.6 Bandkante (naive 0.4, naive_p25 0.3)** · `actions_to_first_craft` 34.5 (flach) · `craft_variety` 5.0 · `feedback_quality` 1.0 · `content_reachable` 1.0 (18/18, keine dangling) · `blueprint_reachability` 1.0 (10/10) · `forage_pressure 0.707 (über Band, Probe zu Ende, Entscheidung am So) · `warmth_stability` 0.46 (Probe bis 27.08.) · `recovery_stability` 0.375 (Probe bis 03.09.). Rest flach.

## Fazit für Direktor/Peter
- **Die Langeweile-Stelle bleibt klein** — die gesamte entdeckbare Welt passt in ~15–19 gezielte Aktionen für einen kompetenten Player.
- Der v2-Wert (64.5) ist eine Re-Leiste (messender Bot), keine gewachsene Länge. Der echte Boredom-Punkt (guided exhaustion) bleibt ~15.
- `discovery_gap` exakt 0.6 = Spiel-Signal beobachten, nicht optimieren.
- Keine Spielqualität-Regression, kein neuer Bug → kein neuer 🔴 B-Eintrag.

## Play-Tooling
- `play/guided_full.py` bleibt HEAD, reconcileiert mit allen 9 Prozessen. 8/20 Volle = dokumentierter Fragile-Zustand, kein Sessions-Regress. Sweep einmal pro Seed (doppelte Calls korrumpieren den RNG-Strom, doc-bemacht 21.08.).