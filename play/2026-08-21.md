# Playtest 2026-08-21

## Headline-Befund: Die Langeweile-Stelle ist unverändert — `session_depth` 25, flach seit fünf Messungen. Die Discovery-Decke ist das Spiel (und sie endet in ~14 geführten Aktionen)

Scorecard-Tag nach SPEC-009 (Verletzung & Heilung, 20.08.): **alle Metriken ±0, bis auf die neue `recovery_stability`-Baseline (0.375, im Band 0.3–0.7).** Deterministische Seeds + unveränderte Engine = zu erwartende Flachheit, kein Regress-Signal.

Der Befund, der zuerst zu nennen ist: **die Langeweile-Stelle existiert weiterhin genau dort**, wo sie seit vier Wochen steht. `session_depth` = 25. Die beiden Messwege decken sich wieder:
- **Geführte Erschöpfung** (volle Tech-Bäume geleert): ein kompetenter Player leert alle 10 Blueprints + 5 Prozesse in **~14–16 Aktionen** (Seeds 20260810/20260812: exhaustion@16/@14), danach `gather_at(random)` = reiner Grind. Die komplette entdeckbare Welt passt in unter 20 gezielte Aktionen.
- **Naive Metrik** 25 (flach).

Die drei Guides zeigen das Muster exakt: `spear → spear_bound → rope → cord_spear → make_bandage → …` alles in 8–16 Aktionen, dann nichts Neues mehr. Das ist der Inhalt selbst, nicht Spieler-Können. Der Tier-2-Layer (rope→cord_spear) rückt die Erschöpfung **nicht** nach hinten — er landet mitten in der Kette (identisch zum 19.08., nur jetzt in Aktionen nachvollziehbar).

**Warum session_depth gegen den Fortschritt immun bleibt (Spiel-Daten bestätigen 18./19.08.):** der naive Random-Bot erreicht das survival-Gate (≥2 Tier-1-Discoveries) vor seinem Stall selten → die 2 neuen Blueprints erscheinen ihm nie; der geführte Bot öffnet das Gate automatisch → Tier-2 im Mittelteil, Erschöpfungspunkt unverändert. Beide Messwege sind strukturell blind für gestufte Discovery. Entscheid liegt beim Direktor/Peter (Metrik = Verfassungs-Kern) — keine stille Änderung.

## Zweitbefund: guided-Bot bleibt fragil, aber das ist der bekannte Zustand — 6/20 voll (19.08: 8/20). Kein neuer Regress durch SPEC-009

Der Bot erreicht alle 10 Blueprints in **6/20** Seeds (19.08.: 8/20). Der Rest stirbt an Kälte-Hunger, die meisten **unter 13 Aktionen**. Kill-Ursache analysiert und **nicht** SPEC-009: die sterbenden Runs haben `injuries={}` — der V-Verbands-Treatment (`_treat_if_injured`) greift seit 20.08 und ist nicht die Todesursache. Es ist derselbe bekannte Kälte-Tod wie vor SPEC-009:

**Die eigentliche Kill-Falle (dokumentierter Wartungsloop):** `_warm_here` zurücktet nur wenn `not fire_active`. Aber der Bot zündet am kalten `mountain_peak` ein Feuer an (fire_active=True) → der Rückzugger-TRigger greift nie, obwohl body_temp bei ~31 bleibt und jede Aktion ~3 HP durch Kälte frisst. Das Feuer hält am Gipfel (STORM/Nacht) nicht warm genug. Manifestiert: seed 20260808 blutet von WP nach −70 mitten im Gipfel.

**Geprüft, bewusst NICHT über-härtet:** Ich habe einen „ruckzug wenn body_temp<35 egal ob Feuer aktiv"-Fix (warm2) real im 20-Sweep getestet → **3/20 voll**, strikt schlechter. Das ist exakt die in der Skill dokumentierte Falle („aggressive early-retreat-Schwellen machen es strikt schlechter, der Bot sammelt nie"). HEAD bleibt die praktische Decke. Referenz für einen zukünftigen chirurgischen Fix (Tech-Debt unten), nicht für die heutige Änderung.

## Scorecard-Referenz (2026-08-21, deterministisch)
- `session_depth` **25.0** (flach, 5. Messung) · `discovery_gap` **0.6** (Bandkante) · `craft_variety` **3.5** · `feedback_quality` **0.916** (NEAR_MISS-Blindstelle, bei Peter) · `warmth_stability` 0.46 (Probe bis 27.08.) · `forage_pressure` 0.707 (Probe bis 20.08., über Band — Ende gestern, Entscheid bei Direktor) · `recovery_stability` **0.375** (Baseline, Probe bis 03.09.) · Rest flach.

## Fazit für Direktor/Peter
- Die Langeweile-Stelle bleibt 25 und das komplette entdeckbare Spiel leert sich in ~15 geführten Aktionen. **Der** zu messende Hebel ist unverändert das Verschieben dieser Decke — am saubersten via Peters GENOs (tool-aware reachability für SPEC-006, oder die session_depth-Bot-Kalibrierung) oder via Dichte des Tiefe statt nur neuer Content-Liste.
- Zwei Metrik-Entscheidungen warten auf Peter: session_depth-Bot-Bedeutung (strukturell blind für gestufte Discovery) + feedback_quality/NEAR_MISS-Mapping (beideres bleibt, siehe BACKLOG).
- Keine Spiel-Qualitäts-Regression durch SPEC-009; die Verletzung ist im Band (`recovery 0.375`), guided-behandelt, Metriken pulsen nicht.

## Play-Tooling
- `play/guided_full.py` bleibt HEAD; keine Änderung. Kötchen des guided-Bot mit 6/20 ist der dokumentierte fragile Zustand, nicht ein neuer Regress.
- Neuer 🔵 Tech-Debt notiert: Rückzug-Trigger-`not fire_active` blockt den Kälte-Retreat am kalten Ort mit „aktivem aber nicht warm genug"-Feuer — chirurgischer Fix (body_temp-gated dort wo das Feuer nicht trägt), sehr vorsichtig, gegen-tastbar über 20-Sweep (der aggressive Test war schlechter).