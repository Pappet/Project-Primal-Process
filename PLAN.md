# Project Primal Process — Plan

> Lebendes Dokument: wird vom Direktor (So 18:00) neu geschrieben.
> Grenze: CONSTITUTION.md.

## Aktueller Zustand
Die Specs SPEC-001/002/004 haben den Einstieg massiv entschärft: `actions_to_first_craft` 63→34.5, `craft_variety` 0.5→3.0, `content_reachable` von 0.667 auf 1.0 (inzwischen 15/15 dank B06/B07-Fix), 170 Tests grün, flint-Funnel weg (naive Sessions bauen jetzt Knochen-/Stein-Varianten). Aber die **Langeweile-Stelle bleibt**: `session_depth`~25 (p75 33→43, Median flach) — die komplette Entdeckungsmenge (8 Blueprints + 4 Prozesse + 15 Templates) ist in ~40 Aktionen geleert, danach existiert schlicht kein Discovery-Ziel mehr. Zentral neu: `discovery_gap` ist durch einen Reachability-Zählfehler **unterschätzt** (gemeldet 0.375, wahr ≈0.625, über Band) — die Metrik ist aktuell nicht verlässlich und muss kalibriert werden, *bevor* man über neue Discovery-Mechaniken entscheidet.

## Was als nächstes besser werden muss
1. **Entdeckungs-Tiefe erhöhen — `session_depth`** (steigend aus ~25). Die Entdeckungs-Leere ist die Langeweile-Stelle; SPEC-004 vertiefte das Sammeln (p75), nicht das Entdecken. Jede neue Mechanik muss Spielern ein *neues* Ziel geben, das über tick ~96 hinaus verspricht. Menge an Discovery verlagern, nicht nur Content anhängen.
2. **Craft-Tiefe ohne Content — `craft_variety`/`session_depth`** halten und verbreitern. `craft_variety`=3.0 ist gut, aber zwei-gleiche-Material-Blueprints (z.B. Speer aus 2×Ast) sind im CLI unspielbar — eine echte Lücke in der Hand, kein Metrikmangel.
3. **Messung zuverlässig machen — `discovery_gap`** Kalibrierung. Der Reachability-Zähler löst Tag-Familien nicht auf (`spear`/`spear_bound` fälschlich unreachable) → wahrer Gap ≈0.625 statt 0.375. **Braucht Peters Freigabe** (Constitution: Metrik-Core unantastbar). Vorrang vor jeder Discovery-Mechanik; SPEC-003 bleibt bis dahin suspendiert.

## Tasks
> Offene Aufgaben mit Akzeptanzkriterien. Dev arbeitet von oben nach unten.
> `[ ]` offen · `[~]` in Arbeit · `[x]` erledigt

- [x] **REC-001 — Reachability-Zähler kalibrieren (`blueprint_reachability`/`discovery_gap`).** ⚠️ **Anwendung braucht weiterhin Peters Freigabe** (Metrik-Berechnung, Constitution). Dev 12.08.: **Patch-Entwurf + Wirkungsabschätzung geliefert** als Vorschlag → `proposals/REC-001-pair-slots-reachability-fix.md`. Ursache: `scorecard.py::_pair_slots` matcht Slot-Tags literal und kennt `TAG_FAMILIES` (`SHARP_OR_RIGID`, `RIGID_OR_FIBER`) nicht → `spear`/`spear_bound` fälschlich unreachable, obwohl die Engine beide craftet (alle 8 Blueprints SUCCESS, verifiziert). Verifizierte Wirkung (n=50/20 Seeds): reachability 0.75→**1.0**, `discovery_gap` 0.375 (im Band) → **0.625 (über Band 0.6)**. **Entwurf nicht angewendet/committet — wartet auf Peters Freigabe.** Gatter für SPEC-003.
- [x] **SPEC-005 — Mengen-basiertes Mehrfach-Slot-Crafting.** Aus BACKLOG 05.08.: `Inventory.add` verschmilzt gleichnamige Items → Zwei-Slot-Blueprints mit 2× demselben Material sind unspielbar (Speer = reeds+Ast statt 2×Ast). Akzeptanz: Stack mit `quantity N` kann N identische Slots füllen (Matching in `execute_experiment`); 2×gleiches Material craftbar; kein Rezept-Leak, Tag-Crafting unverändert; `python -m pytest` grün; `session_depth`/`craft_variety` nicht fallen. (Erledigt: Menge-Validierung in `execute_experiment` + Feedback `NOT_ENOUGH_QUANTITY`, CLI zeigt Inventar mit Mengen.)
- [x] **Skill_spread-Regress klären + Rückwärtsprüfung.** `skill_spread` 0.315→0.216, „höher besser". Klären: echter Regress oder Folge der leichteren Einstiege (mehr Werkzeugpfade, erster Craft früher → optimale vs. zufällige Überlebensspanne schrumpft)? Akzeptanz: Befund im JOURNAL, ob 0.216 echte Spielerfahrung abbildet. Wenn gewollt/fine → Neuinterpretation als Metrik-Vorschlag an Peter (braucht Freigabe); wenn Missmatch → Vorschlag zur Korrektur. Nicht still abschwächen. (Erledigt: Befund im JOURNAL 10.08. — kein Tiefen-Regress, gehobene Einsteiger-Decke; opt-Decke ökonomie-gebunden & stabil. Neuinterpretation als Vorschlag A/B an Peter, wartet auf Freigabe; Metrik unangetastet.)
- [ ] **SPEC-003 — Partielle Match-Erkennung (suspendiert bis REC-001).** Ziel war Gap-Reduktion; solange `discovery_gap` durch den Reachability-Zähler unrealibel ist (wahr ~0.625), lässt sich die Wirkung nicht verifizieren. **Aussetzung bleibt** — Reaktivierung erst nach Kalibrierung neu bewerten (Gefahr beide Band-Ränder, abhängig vom wahren Wert).
- [~] *(beobachtend)* **forage_pressure (Probe bis 20.08.)** — kein Ziel, nur Beobachtung. Erstwert 0.707 über Band 0.1–0.5; `stock < max_stock` ist ein sensibler Schwellenwert. Nach Probezeitende (20.08.) entscheiden, ob Definition/Band zu ändern ist (braucht Peter) oder ob das Spiel reibt.
- [x] **SPEC-007 — Feuer & Wärme: Gegen-Schleife zur Unterkühlung (System-Tiefe, Explorations-Modus).** ✅ Erledigt 13.08. (Dev): aktives Location-Feuer (`fire_active`/`fire_fuel`, FIRE_HEAT 40, Brennstoff verbraucht sich, `FIRE_OUT`-Meldung), `stoke_fire`-Nachlegen (CLI `[w]ärmen`), herstellbarer Fell-Umhang (`fur_cloak`, CLOTHING/insulation 0.6 über `make_fur_cloak`-Prozess), hartes `required_tag_in_env`-Gate (cook_meat braucht akitives Feuer). content_reachable 1.0 (16/16), 189 Tests grün. **Metrik `warmth_stability` aufgenommen** (Probe bis 27.08., Erstwert 0.460, im Band 0.4–0.9).
- [ ] **SPEC-006 — Zweite Entdeckungsschicht: Werkzeug als Zutat (`session_depth`).** ⚠️ **Blockiert — braucht Peters Freigabe (Metrik-seitig).** Dev 11.08.: Spec-Annahme „reachability bleibt 1.0" ist gegen die Implementierung falsch — `metric_reachability` sammelt nur Rohstoffe, baut nie Werkzeuge; Tier-2-Blueprints mit `tool_tag`-Slot (`CUTTING`/`CHOPPING`) sind im Fresh-Gather-Lauf nie erfüllbar → `blueprint_reachability` regrediert (0.75 → 0.545 bei +3 BPs). Kompensation hieße `scorecard._pair_slots` anfassen = Constitution-Core = **Peters Entscheidung**. Reicht über REC-001 hinaus (auch nach Familien-Fix bleibt tool-aware reachability nötig). Optionen A/B/C in JOURNAL 11.08. — erst nach Freigabe umsetzen.

---
*Nächste Scorecard-Kontrolle: nächster Play-Job. Plan-Neufassung: nächster Direktor (So).*